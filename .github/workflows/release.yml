name: Release
on: workflow_dispatch
jobs:
  reload-service:
    runs-on: self-hosted
    steps:
      - name: TP to working directory
        run: cd /home/projects/sudent-tracker && pwd
      - name: Print where am I
        run: pwd